# Agents (Omarchy bar plugin)

Stacked leftover meters and remaining-over-time charts for AI coding
subscriptions. This directory is what `install.sh` copies to
`~/.config/omarchy/plugins/$USER.agents`.

See the repository README for install, screenshots, and how to add an agent.
